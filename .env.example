# Copy this file to .env (which is gitignored) and fill in your values.
# These same variables must be added to Vercel → Project Settings → Environment Variables
# and to GitHub → Repository Settings → Secrets and variables → Actions.

# ── Neo4j Aura (required for /api/sports-db and all ingestion scripts) ────────
# Get these from: https://console.neo4j.io → your instance → "Connect" tab
NEO4J_URI=neo4j+s://xxxxxxxx.databases.neo4j.io
NEO4J_USERNAME=neo4j
NEO4J_PASSWORD=your-password-here

# ── RapidAPI Tennis (optional — enriches player H2H data) ────────────────────
# Get a key from: https://rapidapi.com/api-sports/api/tennis-live-data
RAPIDAPI_KEY=your-rapidapi-key-here
RAPIDAPI_TENNIS_BASE_URL=https://tennisapi1.p.rapidapi.com
RAPIDAPI_TENNIS_HOST=tennisapi1.p.rapidapi.com
RAPIDAPI_TENNIS_SEARCH_PATH=/api/tennis/search/{query}
RAPIDAPI_TENNIS_PLAYER_PATH=/api/tennis/player/{player_id}
